# HANDOFF — まずこれを読む
最終更新: 2026-09-02

## 読む順序
1. このHANDOFF.md  2. state.json  3. 必要時のみ SESSION_LOG.md

## いま何をしているか（1行）
UIブラッシュアップと安定化を master へ push・Pages反映済み。本人の画面で「録音開始ボタンが見えない」と2回報告されたが、こちらの実測では出ており未決着。

## 次にやること（Step 1）
録音ボタンが切れて見えない件を潰す。`.screen` の `min-height:100dvh` を実高さ固定＋フッター固定に変え、
ビューポートがどんな高さでも操作部が必ず画面内に残るようにする（本人の画面で再現できていないため、
`window.innerHeight` と `record-btn` の実測値を先に本人から取ると早い）。改修前後で `node scripts/verify.mjs`。

## 蒸し返し禁止（前提・再調査しない）
- 運用・改修の正本は `~/.claude/skills/realtime-transcribe-app/SKILL.md`。落とし穴と「触ってはいけない箇所」はそこ
- VAD定数・並列数3・表示順の担保方法（先行挿入する空のプレースホルダー）・会話IDをキュー投入時に確定させる設計は変えない
- 本文に「認識中…」を出す方式は本人の指示で廃止済み。進行表示は入力モニターの「変換中」だけ
- 本文の `max-width` は入れない（「左右の空間が無駄」と指摘済み）
- AIによる文章整形はしていない。やっているのは空白・句読点の圧縮と幻聴/重複の除去のみ
- 「直したのに反映されない」はまずPagesのビルド待ち（30〜60秒）とキャッシュ（10分）を疑う

## 応答テンプレ（このまま出す）
状態: UI刷新と安定化はPages反映済み／「録音開始が見えない」が未決着／Step5(PWA化)は未着手
次: フッターを必ず画面内に固定する修正 → 本人の画面で録音ボタンが見えることを確認
